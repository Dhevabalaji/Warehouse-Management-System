import PlaceholderPage from "../shared/PlaceholderPage";

export default function ResetPasswordPage() {
  return (
    <PlaceholderPage
      title="Reset Password"
      subtitle="Create new password screen"
    />
  );
}